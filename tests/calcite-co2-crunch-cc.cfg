[simulation_parameters]
description = "Test input file for batch chem demo driver, using constraints defineded in crunchflow's native input file."
engine = CrunchFlow
engine_inputfile = calcite-co2-crunch.in
initial_condition = initial
delta_t = 0.1 days
num_time_steps = 40;

[state]
density = 997.07511766644416
porosity = 0.5
temperature = 25.0
pressure = 101325.0

[material_properties]
volume = 1.0
saturation = 0.8

[condition]
name = initial

#[condition]
#name = inlet
