[simulation_parameters]
description = "Test input file for batch chem demo driver, using constraints defineded in pflotran's native input file."
engine = PFloTran
engine_inputfile = ion-exchange-valocchi.in
initial_condition = initial
delta_t = 1.0 years
num_time_steps = 1;

[state]
density = 997.16
saturation = 1.0
porosity = 0.5
temperature = 25.0
pressure = 101325.0
cec = 750.0

[material_properties]
volume = 1.0

[condition]
name = initial
