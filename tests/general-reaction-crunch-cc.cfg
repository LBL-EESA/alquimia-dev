[simulation_parameters]
description = "Test input file for batch chem demo driver, using constraints defineded in crunchflow's native input file."
engine = CrunchFlow
engine_inputfile = general-reaction-crunch.in
initial_condition = initial
delta_t = 1.0 d
num_time_steps = 50;

[state]
density = 997.16
porosity = 0.9
temperature = 25.0
pressure = 101325.0

[material_properties]
volume = 1.0
saturation = 1.0

[condition]
name = initial
