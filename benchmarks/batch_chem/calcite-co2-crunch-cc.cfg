[simulation]
description = "Test input file for batch chem demo driver, using constraints defineded in crunchflow's native input file."
timestep = 0.1 days
max_steps = 40;

[state]
density = 997.07511766644416
porosity = 0.5
temperature = 25.0
pressure = 101325.0

[material]
volume = 1.0
saturation = 0.8

[chemistry]
engine = CrunchFlow
input_file = calcite-co2-crunch.in
initial_condition = initial

[output]
verbose = true
type = python